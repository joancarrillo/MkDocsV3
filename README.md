# Projecte MkDocs — UD02 (multi-pàgina en un sol lloc)

- `pip install mkdocs mkdocs-material`
- `mkdocs serve` per previsualitzar
- `mkdocs build` per generar `site/`
- Imatges en `docs/assets/imatges/` (els MD ja apunten a `assets/imatges/...`)
